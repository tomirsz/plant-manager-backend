#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database=mysql
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=plant_manager
spring.datasource.password=plant_manager_password

flyway.url=jdbc:postgresql://localhost:5432/postgres
flyway.user=plant_manager
flyway.password=plant_manager_password

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# DATABASE CONFIGURATION
spring.jpa.database=postgresql
#spring.datasource.url=${JDBC_DATABASE_URL}
#spring.datasource.username=${JDBC_DATABASE_USERNAME}
#spring.datasource.password=${JDBC_DATABASE_PASSWORD}

plantManager.app.jwtSecret=B62tgJYrWCibKh9IoA
plantManager.app.jwtExpirationMs=1800000
java.runtime.version=11

#flyway.url=${JDBC_DATABASE_URL}
#flyway.user=${JDBC_DATABASE_USERNAME}
#flyway.password=${JDBC_DATABASE_PASSWORD}
